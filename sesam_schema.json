{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/open-sesam/sesam/recipients_schema.json",
  "title": "Sesam Schema",
  "description": "Sesam's config file (containing user/groups and secret definitions)",
  "$defs": {
    "pubkey": {
      "type": "string",
      "description": "Public key of this user, either as literal, forge-id, file path or link"
    }
  },
  "type": "object",
  "properties": {
    "version": {
      "description": "The version of this recipients file",
      "type": "integer",
      "default": 1,
      "minimum": 1,
      "maximum": 1
    },
    "users": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "name",
          "pub"
        ],
        "properties": {
          "name": {
            "type": "string",
            "description": "Name of a user - can be email TODO: Maybe enforce mails for future uses?"
          },
          "desc": {
            "type": "string",
            "description": "Description or full name of this user"
          },
          "pub": {
            "description": "Public key(s) of this user. May be given once or several times.",
            "anyOf": [
              {
                "$ref": "#/$defs/pubkey"
              },
              {
                "type": "array",
                "items": {
                  "$ref": "#/$defs/pubkey"
                }
              }
            ]
          }
        }
      }
    },
    "groups": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "string",
          "description": "One of the names described in users above"
        }
      }
    },
    "secrets": {
      "type": "array",
      "items": {
        "description": "Either an include of another sesam.yml or a secret definition with type and path.",
        "oneOf": [
          {
            "type": "object",
            "required": [
              "include"
            ],
            "properties": {
              "include": {
                "type": "string",
                "description": "An include path to another sub sesam.yml file"
              }
            }
          },
          {
            "type": "object",
            "required": [
              "type",
              "path"
            ],
            "properties": {
              "type": {
                "type": "string",
                "description": "What kind of secret TODO: enum with possible values?"
              },
              "path": {
                "type": "string",
                "description": "Path to the decrypted secret"
              },
              "access": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "description": {
                "type": "string",
                "description": "Description of the secret"
              }
            }
          }
        ]
      }
    }
  }
}
